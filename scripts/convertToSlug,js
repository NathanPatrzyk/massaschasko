function convertToSlug(text) {
  return text.toLowerCase()
    .replace(/[^\w ]+/g, "")
    .replace(/ +/g, "-");
}

let text = "Nome da Pessoa"

console.log(text+" -> "+convertToSlug(text))
// CONTINUAR PENSANDO NOS SCRIPTS EM JS PARA USAR NO REACT AO CONSUMIR API SEGUINDO DATABASE ANTIGA...